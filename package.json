{
  "name": "@care-portals/node-json-criteria",
  "version": "3.0.1",
  "description": "Evaluate criteria queries on JSON objects MongoDB style.",
  "main": "lib/index.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('lib', {recursive: true, force: true})\"",
    "build": "node build.js",
    "test": "mocha",
    "prepublishOnly": "npm run clean && npm run build && npm test",
    "publish:dry-run": "npm publish --dry-run",
    "publish:public": "npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/care-portals/node-json-criteria"
  },
  "homepage": "https://github.com/care-portals/node-json-criteria",
  "keywords": [
    "mongo",
    "like",
    "query",
    "criteria",
    "json",
    "evaluate",
    "find"
  ],
  "author": "Forked from Mirek Rusin http://github.com/mirek",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "json-hash": "^1.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "mocha": "^10.8.2"
  }
}
